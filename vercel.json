{
  "buildCommand": "npm run build && cp public/*.mp3 dist/ 2>/dev/null || true && cp public/*.png dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "framework": "vite",

  "functions": {
    "api/**/*.js": {
      "maxDuration": 10
    }
  }
}
