{
  "buildCommand": "npm run build && mv dist/index.html dist/dashboard.html && cp public/index.html dist/index.html && cp public/*.mp3 dist/ 2>/dev/null || true && cp public/*.png dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/dashboard",
      "destination": "/dashboard.html"
    }
  ],
  "functions": {
    "api/**/*.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
